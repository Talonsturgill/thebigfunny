# WORKLOG — faces that move, then an episode worth watching

Written per the house rule: any task too large for one context gets a durable
plan before code, because a plan that lives only in context does not survive
compaction. Resume from the status table and update it after every commit.

## Approved scope (owner, 2026-08-02, after watching case 0002)

Owner verdict, verbatim: *"dee is still just talking sooooo slow like there's no
comedy there its just a monotone person that sounds like they're reading off of a
piece of paper and nothing at all on screen giving any emotion or satire or
anything, also faces don't move no emotion is shown so its like just impossible
to feel anything through this video, on top of the fact that the topic just
turned out to not really be funny."*

Then: *"cool do both, maybe b then a, so our new video has that incorporated, go
slow this is a massive task."*

So: **B first (the expression engine), then A (a new episode that uses it).**

### The three diagnoses this plan answers

1. **Faces do not move.** 3 expression changes in 52 seconds. Emotion is set once
   per Sequence and held for 9 second stretches. Dee changes face exactly once.
2. **Dee reads off a printout.** 6 lines, 5 of them pure document recitation, no
   opinion anywhere. Not a voice problem. No TTS setting fixes a spreadsheet.
3. **The topic was not funny.** The funny critic said 76, "inert in between... it
   buries its own best joke behind a spreadsheet". Shipped at 79.65 against a
   threshold of 78. The machine flagged it and the run shipped anyway.

### Locked decisions, do not relitigate

- 60 seconds, hard gate. Target 50 to 58.
- Ray is RIGHT, never a fool. The Institution never gets a face.
- Punch at institutions only. Slurs / hate / sexual / harassment are bans.
- The cast is fixed. Freshness comes from staging, never from redrawing.
- No claim without a sourced claim-id. Phase 2 kills what cannot be proven.
- Crude and adult is IN (2026-08-02 register change). Toothless caps at 70.

## Status

| # | Task | Status |
| --- | --- | --- |
| **PHASE B — THE EXPRESSION ENGINE** | | |
| B1 | Face track: per-line expression authored in script.json, generated by `scripts/gen_faces_ts.py` into `video-engine/src/` | DONE |
| B2 | Rig: per-emotion eye table + upper lid, plus `flat` and `squint` | DONE |
| B3 | `scripts/face_check.py` gate: max hold, min changes, the listener must REACT | DONE |
| B4 | Wire into `writer.md` + `BIGFUNNY_ROUTINE.md` so every future run must author faces | DONE |
| B5 | Case 0002 as proving ground: 3 changes/0 reactions -> 21/13, frames checked | DONE |
| **PHASE A — CASE 0003** | | |
| A1 | Research the three unused beats | DONE — the-landlord wins |
| A2 | Fact-check to primary documents, produce claims.json | DONE — 9 VERIFIED, 3 CUT |
| A3 | Angle: who-benefits, one step | DONE — awaiting owner read |
| A4 | Script: 19 lines, 55.7s, Dee has a POV, faces on every line | DONE |
| A5 | StairwellBG + Case0003, 8-shot ladder from cue indices, stills checked | DONE |
| A6 | VO fitted, soundcheck PASS (refused one SEDATED line), 3 tracks generated | DONE |
| A7 | Render, look at frames, panel, score | IN PROGRESS |
| A8 | Ship: runs/ artifacts, caption, ledgers, PR, merge | TODO |

## PHASE B design

### B1 — where face direction lives

The mouth comes from the AUDIO because loudness is a measurable fact. An
expression is a WRITING decision, so it belongs in the script, authored by
whoever wrote the line, exactly like a claim-id.

`script.json` lines gain an optional `face` map:

```json
{"t": 9.87, "who": "DEE", "text": "...", "claims": ["c9"],
 "face": {"DEE": "flat", "RAY": "angry"}}
```

Semantics: an entry sets that character's expression FROM this line onward until
something changes it. Both the speaker and the listener may be named, which is
the point: **the listener's face is where the joke lands.**

`scripts/gen_faces_ts.py` (NEW) compiles it into `video-engine/src/`, one file
per case, exporting `emotionAt(who, frame)`, same shape as the other tracks,
same `--check` staleness gate, same self-test.

### B2 — why the rig cannot show emotion today

CORRECTED after reading the rig. A first draft of this section claimed four of
the five expressions share identical brows, from skimming
`browY = emotion === 'shock' ? -14 : 0`. That is false: every emotion draws its
OWN brow path (angry slants in, worried arches out, smug cocks one). Recording
the correction rather than quietly editing it, because the wrong version would
have sent the next context rebuilding a part that works.

What is actually wrong, measured:

1. **The timeline, overwhelmingly.** 3 switch events in 52 seconds. Dee is
   `neutral` for roughly 36 straight seconds because S2 and S3 never set her
   emotion at all and the cast default takes over. This is a SCRIPT and
   COMPOSITION problem, which makes B1 the high-value work, not B2.
2. **Eyes.** Only shock and smug change the eye. `angry`, `worried` and
   `neutral` render an identical eye, so three of five expressions differ by
   brow and mouth alone.
3. **No transition.** Emotion snaps between frames. Real faces move THROUGH a
   change, and the snap is part of why it reads as a slide swap.
4. **Missing registers.** There is no dead flat stare and no suspicious squint,
   which are the two faces this show needs most.

So B2 is smaller than first assumed: per-emotion eyes, a short ease on change,
and the two missing registers. Do not rebuild the brows.

### B3 — the gate

A gate, because a rule nobody can fail is a preference. Hard-fails:
- any character holding ONE expression longer than a whole beat while on screen
- fewer than N expression changes across the episode
- a LISTENER who never reacts during someone else's line

Self-test must go red on each, per the house rule.

## PHASE A — the story, locked

**US v. RentGrow, Inc.**, FTC matter 222 3002, civil action 1:26-cv-02415,
D.D.C., filed 2026-07-09. $2.25 million. Tenant screening.

The FTC alleged RentGrow's reports carried DUPLICATE entries for the same
criminal or eviction case, which made applicants look like they had been evicted
more times than they had. That when consumers asked what was in their file it did
not disclose all its sources, including Lexis-Nexis Accurint, which made the data
hard to dispute. That it sometimes marked a dispute "invalid" and did nothing
further. And that it told property managers something different from what it told
the consumer about the same dispute.

Variety: `the-landlord` has never run. Subject is new. Angle type
`who-benefits` has never been used (used so far: euphemism, ratio).

### THE ANGLE (who-benefits, exactly one step)

> **You are not the tenant screening company's customer. The landlord is.**
> That one step explains every allegation in the filing, including why your
> dispute could be marked invalid while the landlord was told something else.

The pivot fact, and the thing the whole episode is built to arrive at: the same
eviction, counted twice. Per the case 0002 lesson, this is NOT item four in a
data block; it is the turn.

## PHASE A notes

The lesson already in `ledger/instincts.json` from case 0002 and the one to
actually obey this time: **pick the spine by asking which single VERIFIED fact is
funniest, then build the episode to arrive at it.** Do not pick an angle TYPE
first and fit the facts to it.

Dee's job in the new script is a POINT OF VIEW. She may cite, but every line she
speaks has to be a person saying something, not a row from a table read aloud.

## File map

- Rig: `video-engine/src/lib/Character.tsx` (expressions ~line 189-285)
- Mouth track: `scripts/vo_envelope.py` -> `caseNNNN_mouth.ts`
- Caption track: `scripts/gen_captions_ts.py` -> `caseNNNN_captions.ts`
- Faces: `scripts/gen_faces_ts.py` (NEW), emits one face track per case
- Gates: `scripts/face_check.py` (NEW), plus `scripts/script_check.py` and `scripts/vo_soundcheck.py`
- Master routine: `prompts/BIGFUNNY_ROUTINE.md`
- Voice + cast law: `knowledge/CAST_BIBLE.md`
- Scar tissue: `knowledge/FIELD_NOTES.md`

## Carried over, still open

- C: Institution costume system (designed, never built)
- E: trigger config (lives outside this repo)
- Dee's VOICE PICK is still with the owner. An audition of seven candidates was
  sent; `main` carries Pulcherrima. The soundcheck measured every candidate
  between 3.46 and 4.12 semitones of pitch variance including the one already
  called robotic, so the machine cannot rank them and does not pretend to.

Delete this file when both phases are DONE.
