Read https://github.com/Talonsturgill/thebigfunny/blob/main/prompts/BIGFUNNY_ROUTINE.md
from the main branch and execute it end to end for today's date.

That file is the SOURCE OF TRUTH for this routine. Follow it exactly, including
every hard gate. Read it fresh each run; do not work from memory of a previous
run, because the file changes and the whole point of this pointer is that the
behaviour lives in the repo rather than in this box.

Also read CLAUDE.md in the same repo before you start. It carries the delivery
policy and the house rules.

The job is to deliver ONE video. A gate failure ends an attempt, never the run.
Fix the work and go again: take the next story, cut the line, re-render. The
gates never bend and the standard is never lowered to fit what you have.

You are not finished when you have tried. You are finished when a video exists
that cleared every gate.

Never post to a social platform. This routine renders and drafts; a human posts.
